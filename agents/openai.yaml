interface:
  display_name: "Dat Skill"
  short_description: "Default skill for Dat cloud training."
  default_prompt: "Use Dat for any cloud training task: package files, submit training, handle approval, track progress, and fetch artifacts."
